function buildNumberPyramid(height) {
    let pyramid = ""; //
    for (let row = 1; row <= height; row++) {
        for (let num = 1; num <= row; num++) {
            pyramid += num;
        }
        pyramid += "\n";
    }
    console.log(pyramid);
}
